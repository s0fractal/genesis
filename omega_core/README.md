# OMEGA-64 Core (Legacy Archive)

**Status:** ARCHIVED (Era 2070+)

This directory contains the original, floating-point `omega_core` physics engine. It has been superseded by `omega_v2`, which implements the deterministic, zero-copy, integer-only Q-scale mathematics required for STARK-provable ZK consensus.

**Do not modify this code.** It is preserved for forensic purposes and cross-validation of earlier architectural eras.

For the active physics kernel, see `omega_v2/`.
