# OMEGA-64 Vision

Canonical topological source:

`../src/x7F00_global_swarm_vision.myc.md`

This file is a compatibility pointer for readers and tools that still open
`omega/docs/VISION.md`. The vision itself now lives in `omega/src/` so it can
participate in the same source-addressed projection flow as code and protocol
artifacts.
