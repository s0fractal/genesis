# Task 0185: Era 1990 - Translation Policy Replay Digest Digest Forensic Event Adapter

## Status

✅ COMPLETED

## Context

Era 1980 makes the newer `tpdq` replay-interpretation digest quorum
visible to operators, but that state was not yet recorded into forensic
event sinks.

## Implementation

- Added
  `src/network/translation_policy_replay_digest_digest_forensic_event_adapter.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_DIGEST_FORENSIC_EVENT_ADAPTER_SCHEMA =
  "OMEGA-1990/v1"`.
- Added `TranslationPolicyReplayDigestDigestForensicEventAdapter`.
- Added factory and option merge helpers.
- Added compact `tpdd` payload builder.
- Added stable projection hashing:
  - excludes wall-clock `emitted_at_ms`;
  - includes HUD band, consensus digest/count, total claims,
    agreement Q16, distinct digest count, dissenter count, malformed
    count, and local emit counters.
- Added skip-unchanged behavior.

## Tests

- `tests/translation_policy_replay_digest_digest_forensic_event_adapter_test.ts`
  - disabled adapter inert;
  - start/stop active state;
  - compact `tpdd` append;
  - skip unchanged projection;
  - changed consensus append;
  - drift/blocked capture;
  - projection ignores emitted time;
  - malformed/local counters affect projection;
  - custom kind;
  - last event hash telemetry;
  - append error reporting;
  - null sink factory;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_digest_forensic_event_adapter.ts tests/translation_policy_replay_digest_digest_forensic_event_adapter_test.ts
deno test --allow-read tests/translation_policy_replay_digest_digest_forensic_event_adapter_test.ts
```

Targeted result: `13 passed | 0 failed`.

## Invariant Notes

- Explicit adapter only.
- No DOM writes.
- No mesh IO.
- No timers owned by adapter.
- Stable projection excludes wall-clock emission time.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 2000: Translation Policy Replay Digest Digest Forensic Replay.**
Era 1990 writes compact `tpdd` forensic events, but synced sinks still
need a pure replay classifier for reconstructing digest-digest quorum
history: band timelines, consensus digest intervals, error windows,
first/last time, final band, and final consensus digest.
