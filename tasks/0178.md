# Task 0178: Era 1920 - Translation Policy Replay Digest Forensic Replay

## Status

✅ COMPLETED

## Context

Era 1910 writes compact `tpdq` forensic events, but synced sinks still
lacked a pure replay classifier for reconstructing replay-digest quorum
history.

## Implementation

- Added `src/network/translation_policy_replay_digest_forensic_replay.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_FORENSIC_REPLAY_SCHEMA =
  "OMEGA-1920/v1"`.
- Added replay entry points:
  - `classifyTranslationPolicyReplayDigestForensicSink(...)`;
  - `classifyTranslationPolicyReplayDigestForensicEvents(...)`.
- Added strict payload parser:
  - `parseTranslationPolicyReplayDigestForensicPayload(...)`.
- Reconstructs:
  - HUD/quorum band timeline;
  - consensus digest intervals with count/total/agreement Q16;
  - drift windows;
  - malformed windows;
  - local-claim-failure windows;
  - first/last event time;
  - final band and consensus digest.

## Tests

- `tests/translation_policy_replay_digest_forensic_replay_test.ts`
  - empty classification;
  - ignored/malformed accounting;
  - deterministic band timeline;
  - consensus digest intervals;
  - drift and local failure windows;
  - malformed windows;
  - custom kind;
  - payload parser rejection;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_forensic_replay.ts tests/translation_policy_replay_digest_forensic_replay_test.ts
deno test --allow-read tests/translation_policy_replay_digest_forensic_replay_test.ts
```

Targeted result: `9 passed | 0 failed`.

## Invariant Notes

- Pure replay.
- No sink mutation.
- No DOM writes.
- No mesh IO.
- Deterministic event ordering by `sunk_at_ms`, `sequence`, `event_hash`.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1930: Translation Policy Replay Digest Replay HUD.** The replay
classifier now reconstructs `tpdq` history, but operators still need a
compact formatter for offline/synced-sink reports: final quorum band,
final consensus digest, digest interval count, first active error,
drift duration, and malformed/local-failure totals.
