# Task 0179: Era 1930 - Translation Policy Replay Digest Replay HUD

## Status

✅ COMPLETED

## Context

Era 1920 reconstructs `tpdq` forensic history, but synced/offline sinks
still lacked compact operator-facing report fields for the replay-digest
quorum state.

## Implementation

- Added
  `src/network/translation_policy_replay_digest_forensic_replay_hud.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_FORENSIC_REPLAY_HUD_SCHEMA =
  "OMEGA-1930/v1"`.
- Added `formatTranslationPolicyReplayDigestForensicReplayHud(...)`.
- Added `translationPolicyReplayDigestForensicReplayHudFields(...)`.
- Added replay-report helpers for:
  - final quorum band + glyph;
  - final consensus digest + interval count;
  - first active error window;
  - drift duration;
  - malformed totals;
  - local-claim-failure totals;
  - deterministic summary truncation.

## Tests

- `tests/translation_policy_replay_digest_forensic_replay_hud_test.ts`
  - empty replay classification fields;
  - compact final band/digest/interval/drift/failure fields;
  - active error surfacing;
  - open drift segment duration;
  - malformed total accounting;
  - local failure accounting;
  - field ordering;
  - deterministic truncation;
  - glyph mapping;
  - inactive error handling;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_forensic_replay_hud.ts tests/translation_policy_replay_digest_forensic_replay_hud_test.ts
deno test --allow-read tests/translation_policy_replay_digest_forensic_replay_hud_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- Pure formatter.
- No DOM writes.
- No sink mutation.
- No mesh IO.
- No live runtime subscriptions.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1940: Translation Policy Replay Digest Replay Digest.** Offline
`tpdq` replay interpretations are now report-formatted but not
content-addressed. Add a deterministic FNV-1a digest over replay
classification/HUD state (band timeline, consensus intervals, error
windows, final band/digest) so relays can compare offline replay
interpretations without shipping full timelines.
