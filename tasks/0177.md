# Task 0177: Era 1910 - Translation Policy Replay Digest Forensic Event Adapter

## Status

✅ COMPLETED

## Context

Era 1900 made replay-digest quorum operator-readable, but quorum drift
and consensus changes were not yet recorded into the forensic event
sink.

## Implementation

- Added `src/network/translation_policy_replay_digest_forensic_event_adapter.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_FORENSIC_EVENT_ADAPTER_SCHEMA =
  "OMEGA-1910/v1"`.
- Added `TranslationPolicyReplayDigestForensicEventAdapter`:
  - explicit `handleSnapshot(snapshot, telemetry)` entry point;
  - optional `enabled` gate;
  - compact default `kind = "tpdq"`;
  - start/stop active telemetry;
  - FNV-1a event hash over a stable projection;
  - skip unchanged projection;
  - append-failure telemetry.
- Added payload/projection helpers:
  - `translationPolicyReplayDigestForensicPayload(...)`;
  - `translationPolicyReplayDigestForensicProjection(...)`;
  - create/merge option helpers.

## Tests

- `tests/translation_policy_replay_digest_forensic_event_adapter_test.ts`
  - disabled inert behavior;
  - active lifecycle;
  - compact `tpdq` append;
  - unchanged projection skip;
  - consensus change append;
  - drift/blocked payload bands;
  - emitted-time projection insensitivity;
  - malformed/local counter projection sensitivity;
  - custom kind;
  - last hash telemetry;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_forensic_event_adapter.ts tests/translation_policy_replay_digest_forensic_event_adapter_test.ts
deno test --allow-read tests/translation_policy_replay_digest_forensic_event_adapter_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- No DOM writes.
- No mesh IO.
- No timers or polling ownership.
- Forensic sink append only happens through explicit caller invocation.
- Projection excludes wall-clock `emitted_at_ms`.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1920: Translation Policy Replay Digest Forensic Replay.** The
quorum now writes compact `tpdq` events, but there is no replay
classifier for those events. Add a pure replay module that reconstructs
consensus-digest intervals, drift windows, malformed/local-failure
windows, and final quorum band from synced forensic sinks.
