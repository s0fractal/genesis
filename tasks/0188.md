# Task 0188: Era 2020 - Translation Policy Replay Digest Digest Forensic Replay HUD

## Status

✅ COMPLETED

## Context

Era 2010 gives offline `tpdd` replay interpretations a compact digest,
but operators still needed a compact formatter for reading those
conclusions without parsing full timelines.

## Implementation

- Added
  `src/network/translation_policy_replay_digest_digest_forensic_replay_hud.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_DIGEST_FORENSIC_REPLAY_HUD_SCHEMA =
  "OMEGA-2020/v1"`.
- Added stable HUD snapshot formatter.
- Added four HUD-ready fields:
  - `TPOL RDD REPLAY`;
  - `TPOL RDD DIGEST`;
  - `TPOL RDD ERROR`;
  - `TPOL RDD DRIFT`.
- Surfaced:
  - final band;
  - final consensus digest;
  - consensus interval count;
  - Era 2010 replay digest hex;
  - active error;
  - drift duration;
  - malformed count;
  - local failure count.
- Added deterministic summary truncation.

## Tests

- `tests/translation_policy_replay_digest_digest_forensic_replay_hud_test.ts`
  - empty classification fields;
  - compact field formatting;
  - active error surfacing;
  - open drift duration;
  - malformed count aggregation;
  - local failure count;
  - helper field order;
  - deterministic truncation;
  - glyph coverage;
  - closed-error null active helper;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_digest_forensic_replay_hud.ts tests/translation_policy_replay_digest_digest_forensic_replay_hud_test.ts
deno test --allow-read tests/translation_policy_replay_digest_digest_forensic_replay_hud_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- Pure formatter only.
- No DOM writes.
- No sink mutation.
- No mesh IO.
- No timers or wall-clock reads.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 2030: Translation Policy Replay Digest Digest Claim from Forensic
Replay Digest.** Era 2020 makes offline `tpdd` replay interpretations
operator-readable, but relays still need a wire-ready claim shape for
sharing the Era 2010 replay digest itself through the existing mesh
claim/quorum pattern.
