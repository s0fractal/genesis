---
task_id: "0187"
idea_id: "legacy-0187"
state: "ORGAN"
origin:
  substrate: "local"
  uri: "tasks/0187.md"
phase_vector:
  consensus: 0
  kinematics: 0
  codeicide: 0
warrant_required: false
---

# Task 0187: Era 2010 - Translation Policy Replay Digest Digest Forensic Replay Digest

## Status

✅ COMPLETED

## Context

Era 2000 reconstructs `tpdd` forensic replay history, but independent
relays still needed a compact deterministic digest of that interpretation
so they can compare conclusions without shipping full timelines.

## Implementation

- Added
  `src/network/translation_policy_replay_digest_digest_forensic_replay_digest.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_DIGEST_FORENSIC_REPLAY_DIGEST_SCHEMA =
  "OMEGA-2010/v1"`.
- Added whole-classification digest helper.
- Added component hashes for:
  - band timeline;
  - consensus digest intervals;
  - error windows.
- Added canonical projection helper.
- Added equality helper for relay interpretation comparison.
- Digest includes interpretation fields only:
  - component hashes;
  - classified event count;
  - malformed payload count;
  - final band;
  - final consensus digest.
- Digest intentionally ignores non-interpretive bookkeeping:
  `total_events` and `ignored_events`.

## Tests

- `tests/translation_policy_replay_digest_digest_forensic_replay_digest_test.ts`
  - empty digest shape;
  - deterministic repeated calls;
  - ignores non-interpretive bookkeeping;
  - changes on band timeline drift;
  - changes on consensus interval drift;
  - changes on error-window drift;
  - component hash parity;
  - canonical projection sections;
  - same-helper comparison;
  - malformed count is interpretive;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_digest_forensic_replay_digest.ts tests/translation_policy_replay_digest_digest_forensic_replay_digest_test.ts
deno test --allow-read tests/translation_policy_replay_digest_digest_forensic_replay_digest_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- Pure function over Era 2000 classification.
- No sink mutation.
- No DOM writes.
- No mesh IO.
- No timers or wall-clock reads.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 2020: Translation Policy Replay Digest Digest Forensic Replay
HUD.** Era 2010 gives offline `tpdd` replay interpretations a compact
digest, but operators still need a compact HUD formatter for final band,
consensus digest intervals, drift duration, malformed count, local
failure count, and digest hex.
