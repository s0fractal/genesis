---
task_id: "0180"
idea_id: "legacy-0180"
state: "ORGAN"
origin:
  substrate: "local"
  uri: "tasks/0180.md"
phase_vector:
  consensus: 0
  kinematics: 0
  codeicide: 0
warrant_required: false
---

# Task 0180: Era 1940 - Translation Policy Replay Digest Replay Digest

## Status

✅ COMPLETED

## Context

Era 1930 made offline `tpdq` replay classifications readable, but relays
still lacked a compact content address for comparing interpretation
outcomes without exchanging full timelines.

## Implementation

- Added
  `src/network/translation_policy_replay_digest_forensic_replay_digest.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_FORENSIC_REPLAY_DIGEST_SCHEMA =
  "OMEGA-1940/v1"`.
- Added `translationPolicyReplayDigestForensicReplayDigest(...)`.
- Added `sameTranslationPolicyReplayDigestForensicReplayDigest(...)`.
- Added `translationPolicyReplayDigestForensicReplayDigestProjection(...)`.
- Added independent FNV-1a component hashes for:
  - replay-digest band timelines;
  - consensus digest intervals;
  - replay-digest error windows.

## Tests

- `tests/translation_policy_replay_digest_forensic_replay_digest_test.ts`
  - empty digest shape;
  - repeat determinism;
  - non-interpretive bookkeeping ignored;
  - band timeline sensitivity;
  - consensus interval sensitivity;
  - error window sensitivity;
  - component hash parity;
  - canonical projection sections;
  - same-helper matching;
  - malformed-payload count sensitivity;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_forensic_replay_digest.ts tests/translation_policy_replay_digest_forensic_replay_digest_test.ts
deno test --allow-read tests/translation_policy_replay_digest_forensic_replay_digest_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- Pure digest.
- No DOM writes.
- No sink mutation.
- No mesh IO.
- Ignores non-interpretive `total_events` and `ignored_events`.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1950: Translation Policy Replay Digest Digest Claims.** The replay
digest interpretation is now content-addressed locally, but peers still
cannot exchange this newer `tpdq` interpretation digest directly. Add a
compact claim/wire helper over Era 1940 digests so relays can advertise
offline replay interpretation agreement without reusing the older Era
1870 claim shape.
