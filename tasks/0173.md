# Task 0173: Era 1870 - Translation Policy Replay Digest Wire Claim

## Status

✅ COMPLETED

## Context

Era 1860 content-addressed translation-policy forensic replay
interpretations, but peers still had no compact way to announce their
interpretation anchor after forensic sink sync.

## Implementation

- Added `src/network/translation_policy_replay_digest_claim.ts`.
- Introduced `TRANSLATION_POLICY_REPLAY_DIGEST_CLAIM_SCHEMA =
  "OMEGA-1870/v1"`.
- Added `TranslationPolicyReplayDigestClaim`, carrying:
  - replay digest schema;
  - peer and witness ids;
  - full digest plus component hashes;
  - classified/malformed counts;
  - final band and policy hash;
  - claim timestamp.
- Added helpers:
  - `buildTranslationPolicyReplayDigestClaim(...)`;
  - `decodeTranslationPolicyReplayDigestClaim(...)`;
  - `translationPolicyReplayDigestClaimMatchesDigest(...)`;
  - `sameTranslationPolicyReplayDigestClaim(...)`;
  - `translationPolicyReplayDigestPlasmidFields(...)`.
- Added `TranslationPolicyReplayDigestClaimBridge` as a minimal
  transport shim with send counters and receiver-side decode.
- Extended `PlasmidPayload.semanticType` with
  `TRANSLATION_POLICY_REPLAY_DIGEST`.
- Added passive `translationPolicyReplayDigestClaim` dispatch in
  `webrtc_v2.ts`; application-owned consumers validate and apply.

## Tests

- `tests/translation_policy_replay_digest_claim_test.ts`
  - claim construction from Era 1860 digest;
  - JSON round-trip;
  - malformed decoder rejection;
  - plasmid field encoding;
  - bridge send counters;
  - emit-failure behavior;
  - receiver decode;
  - local digest match helper;
  - interpretation equality across peer/witness/time;
  - drift sensitivity;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_claim.ts tests/translation_policy_replay_digest_claim_test.ts
deno test --allow-read tests/translation_policy_replay_digest_claim_test.ts
```

Targeted result: `11 passed | 0 failed`.

## Invariant Notes

- Pure JSON wire claim.
- No sink mutation.
- No automatic network IO.
- Mesh delivery is passive event dispatch only.
- Existing replay digest remains the source of truth.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1880: Translation Policy Replay Digest Corroboration Tracker.**
Peers can now announce compact replay interpretation anchors. The next
step is to collect these claims per digest, classify agreement bands
(`lone`/`double`/`triple+`/`high`), and expose dissenters when relays
replay the same synced forensic history but disagree on interpretation.
