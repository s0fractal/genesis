# Task 0090: Era 1040 — ZK-Notarized Mutations

## Status: PHASE-1-COMPLETED + PHASE-2-COMPLETED | PHASE-3-OPTIONAL (full SP1 prover) | Priority: HIGH

> This task was proposed by the lattice itself via the Era 1030 Senate
> on 2026-04-25. Hash: `0xFAA7_FF6E` (FNV-1a 32-bit, zero-padded
> 64-byte UTF-8). It awaits 3+ AYE votes to be ratified.

## Context
Era 1030 (Autopoietic Legislation) is complete. The Senate is operational
with deterministic FNV-1a hashing, dipole-validated voting, and
materialization to localStorage + downloadable task artifacts.

## Objective
Repair `omega_zk_guest` and wire SP1 STARK proofs into the live mutation
pipeline. Every `darwinian_mitosis` event MUST produce a verifiable proof
that the mutation followed the integer-deterministic physics laws.
Mutations crossing the WebRTC mesh without a valid receipt MUST be
rejected at the boundary.

## Acceptance Criteria

### 1. ZK Guest Resurrection ✅ (Phase 1)
- [x] `omega_zk_guest/Cargo.toml` excludes the binary from host
  `cargo test` (kept for `cargo prove build` / SP1 toolchain).
- [x] Mode 0 (PoUW) and Mode 1 (Resonance) preserved.
- [x] Mode 2 added in `src/main.rs`: reads parent + attractor array +
  claimed child, re-derives via `derive_mitosis_child`, asserts bit-for-bit
  equality, commits `(mode, parent_genome, attractor_count, receipt)`.

### 2. Pure Mitosis Derivation ✅ (Phase 1)
- [x] `omega_v2/src/mitosis_proof.rs` — pure, deterministic
  `derive_mitosis_child(parent, attractors, q_phase) -> child`.
  Used identically by `lattice::darwinian_mitosis` AND
  `omega_zk_guest::main` Mode 2.
- [x] `child_receipt_hash(child)` — FNV-1a 32-bit over LE-encoded 32-byte
  agent, the same algorithm as the Era 1030 senate hash.
- [x] 9 unit tests pass under `cargo test -p omega_v2 mitosis_proof`.

### 3. Cross-Language Anchors ✅ (Phase 1)
- [x] `omega_v2/tests/mitosis_anchor.rs` fixes two anchor receipts:
  `0xD434_E690` (no attractors) and `0x3B88_1A47` (dominant attractor).
- [x] `tests/mitosis_proof_test.ts` mirrors the same anchors against the
  `src/network/mitosis_proof.ts` JS port. JS produces identical receipts
  for identical inputs.
- [x] `src/network/mitosis_proof.ts` — pure-TS port of the derivation
  function and receipt hasher.

### 4. Mesh Boundary Verification ✅ (Phase 1)
- [x] `PlasmidPayload` extended with `parent`, `claimedChild`,
  `attractors`, `qPhase`, `receiptHash` (DIPOLE only).
- [x] `WebRTCV2Mesh.verifyMitosisProof` rejects DIPOLE plasmids whose
  claimed child does not match local re-derivation.
- [x] `verifiedDipoleCount` accumulates across the mesh; HUD slot `f`
  shows live count.
- [x] Era 1050 trigger fires at 100 verified proofs.

### 5. Host-Side Parent Snapshotting (Phase 2) ✅
- [x] `omega_v2/src/mitosis_log.rs`: static MITOSIS_LOG ring buffer
  (capacity 32) of `MitosisReceipt { parent, child, attractors, q_phase,
  receipt_hash, tick }`. Each entry is 160 bytes, zero-allocation.
- [x] `darwinian_mitosis` snapshots parent BEFORE energy debit AND BEFORE
  child overwrites the dead slot, then appends the full receipt.
- [x] FFI: `v2_mitosis_log_ptr`, `v2_mitosis_log_total`,
  `v2_mitosis_log_capacity`, `v2_mitosis_log_clear`.
- [x] `src/network/mitosis_log_reader.ts`: drainMitosisLog handles
  monotonic order, writer-overflow, lastSeen tracking.
- [x] `bootstrap/v2.ts`: birth scan replaced — drains the log per GPU
  tick, packages each receipt as a fully-verifiable DIPOLE plasmid with
  parent + claimedChild + attractors + qPhase + receiptHash. Local
  sanity check refuses to broadcast on hash mismatch.
- [x] FFI integration test (`omega_v2/tests/mitosis_log_integration.rs`)
  drives the full `boot → big_bang → seed thriving → sweep → drain →
  re-derive → verify` pipeline.

### 6. Full SP1 Prover (Phase 3 — OPTIONAL)
- [ ] `tools/zk_prove_mitosis.ts` (Deno) takes a `MitosisReceipt` and
  produces a real SP1 STARK proof bundle via `cargo prove`.
- [ ] `cargo prove build` reproducibly compiles `omega_zk_guest` to
  `riscv32im-succinct-zkvm-elf`. (Mode 2 is already in place — only the
  toolchain hookup is missing.)
- [ ] Round-trip CI: prove a known mitosis triple in Mode 2, then verify
  the receipt from JS via SP1's WASM verifier. Replaces local
  `verifyMitosisProof` with cryptographic verification.

> Phase 3 is gated only by SP1 toolchain availability in CI. The Rust
> side is ready: every `darwinian_mitosis` event already produces a
> `MitosisReceipt` with the exact 5-tuple Mode 2 expects on stdin.

## Provenance
- Proposer matrix: TBD (set on first AYE vote received)
- Proposed at: 2026-04-25
- AYE votes: 1 (lattice self-vote) — Phase 1 work has been completed
  in advance; Phase 2 awaits SP1 toolchain availability.
- Description hash: `0xFAA7_FF6E`

## Related Files
- `omega_zk_guest/src/main.rs` (currently shipped, needs Mode 2)
- `omega_v2/src/lattice.rs::darwinian_mitosis`
- `omega_v2/src/pouw.rs` (existing PoUW model)
- `src/network/webrtc_v2.ts` (mesh boundary)

## Next Era Trigger
On 100 successfully-verified DIPOLE plasmids with valid proofs, **Era 1050:
Open Protocol Stamping** unlocks. RFC-OMEGA-001 v1.0 freezes; the wire
format is anchored to a Bitcoin OP_RETURN inscription.
