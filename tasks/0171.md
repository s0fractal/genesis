# Task 0171: Era 1850 - Translation Policy Forensic Replay HUD Summary

## Status

✅ COMPLETED

## Context

Era 1840 reconstructs policy-band timelines, policy-hash intervals, and
error windows from synced `tpol` forensic events. Operators still needed
a compact display/report artifact instead of reading the full
classification object.

## Implementation

- Added `src/network/translation_policy_forensic_replay_hud.ts`.
- Introduced `TRANSLATION_POLICY_FORENSIC_REPLAY_HUD_SCHEMA =
  "OMEGA-1850/v1"`.
- Added `formatTranslationPolicyForensicReplayHud(classification, opts)`
  returning:
  - final replay band + compact glyph;
  - final policy hash and policy interval count;
  - first active error window;
  - total drift duration;
  - malformed aggregate;
  - deterministic summary string;
  - four HUD/report-ready fields.
- Added helpers:
  - `translationPolicyForensicReplayHudFields(...)`;
  - `translationPolicyReplayDriftDurationMs(...)`;
  - `translationPolicyReplayMalformedCount(...)`;
  - `firstActiveErrorWindow(...)`;
  - `forensicReplayGlyph(...)`.

## Tests

- `tests/translation_policy_forensic_replay_hud_test.ts`
  - empty classification formatting;
  - compact field content;
  - first active error surfacing;
  - open drift duration;
  - malformed aggregate;
  - helper field order;
  - deterministic truncation;
  - glyph coverage;
  - no-active-error handling;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_forensic_replay_hud.ts tests/translation_policy_forensic_replay_hud_test.ts
deno test --allow-read tests/translation_policy_forensic_replay_hud_test.ts tests/translation_policy_forensic_replay_test.ts
```

Targeted result: `20 passed | 0 failed`.

## Invariant Notes

- Pure formatter only.
- No DOM writes.
- No sink mutation.
- No mesh/network IO.
- ASCII glyphs keep replay reports compact and terminal-safe.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1860: Translation Policy Forensic Replay Digest.** The replay HUD
is readable, but classification outputs are not yet content-addressed.
Add a deterministic digest over band timeline, policy intervals, and
error windows so two relays can prove their replay interpretation
matches after event-sink sync.
