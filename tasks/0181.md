# Task 0181: Era 1950 - Translation Policy Replay Digest Digest Claims

## Status

✅ COMPLETED

## Context

Era 1940 made `tpdq` replay interpretations content-addressed locally,
but peers still could not advertise this newer digest directly over the
mesh without reusing the older Era 1870 `tpol` claim shape.

## Implementation

- Added `src/network/translation_policy_replay_digest_digest_claim.ts`.
- Introduced
  `TRANSLATION_POLICY_REPLAY_DIGEST_DIGEST_CLAIM_SCHEMA =
  "OMEGA-1950/v1"`.
- Added compact claim builders/decoders:
  - `buildTranslationPolicyReplayDigestDigestClaim(...)`;
  - `decodeTranslationPolicyReplayDigestDigestClaim(...)`.
- Added local comparison helpers:
  - `translationPolicyReplayDigestDigestClaimMatchesDigest(...)`;
  - `sameTranslationPolicyReplayDigestDigestClaim(...)`.
- Added a minimal bridge:
  - `TranslationPolicyReplayDigestDigestClaimBridge`.
- Added plasmid field helper:
  - `translationPolicyReplayDigestDigestPlasmidFields(...)`.
- Added passive mesh dispatch in `webrtc_v2.ts`:
  - semantic type `TRANSLATION_POLICY_REPLAY_DIGEST_DIGEST`;
  - event `translationPolicyReplayDigestDigestClaim`.

## Tests

- `tests/translation_policy_replay_digest_digest_claim_test.ts`
  - compact claim construction;
  - JSON round-trip;
  - malformed decode rejection;
  - plasmid field encoding;
  - bridge success/failure accounting;
  - incoming decode path;
  - exact local digest match;
  - equality ignoring peer/witness/time;
  - interpretation drift detection;
  - invalid claim time normalization;
  - schema constant.

## Verification

```bash
deno check src/network/translation_policy_replay_digest_digest_claim.ts tests/translation_policy_replay_digest_digest_claim_test.ts
deno test --allow-read tests/translation_policy_replay_digest_digest_claim_test.ts
```

Targeted result: `12 passed | 0 failed`.

## Invariant Notes

- Pure claim/decoder layer.
- Passive mesh delivery only.
- No sink mutation.
- No DOM writes.
- No live runtime ownership.
- No Rust/WASM/WGSL path touched.

## Next Resonant Step

**Era 1960: Translation Policy Replay Digest Digest Quorum.** Era 1950
claims can now move over mesh, but there is no tracker for agreement over
the newer `tpdq` replay-interpretation digests. Add a TTL-bounded quorum
tracker over `TranslationPolicyReplayDigestDigestClaim` with deterministic
consensus selection, dissenters, agreement Q16, and lone/double/triple+/high
bands.
