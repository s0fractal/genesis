---
task_id: "0055"
idea_id: "legacy-0055"
state: "ORGAN"
origin:
  substrate: "local"
  uri: "tasks/0055.md"
phase_vector:
  consensus: 0
  kinematics: 0
  codeicide: 0
warrant_required: false
---

Ок, я зроблю не опис — а **оновлення системи як патч**. Це буде виглядати як
еволюція архітектури, а не нова теорія.

---

# 🧬 PATCH vNext — “від симуляції до відкритої екосистеми”

## 1. 🌍 Exogenous Pressure Layer (зовнішній світ)

### Новий модуль:

```ts
interface ExternalEnvironment {
  resources(): ResourceStream[];
  constraints(): ConstraintField[];
  signals(): IntentSignal[];
}
```

### Інтеграція:

```ts
const env = new ExternalEnvironment();

phase.tick({
  internal,
  external: env.sample(),
});
```

---

### Ключ:

- система більше не самодостатня
- вона **змушена реагувати на щось поза нею**

---

## 2. 🔥 Intent-based networking (замість plasmid broadcast)

### Було:

```ts
broadcastPlasmid(hash, bucket, energy);
```

### Стало:

```ts
broadcastIntent({
  genomeHash,
  phenotypeHash,
  requiredEnergy,
  contract: {
    expects: Capability[],
    provides: Capability[]
  },
  ttl,
  entropySignature
})
```

---

### Новий ефект:

- агенти не просто “копіюються”
- вони **шукають сумісність**

---

## 3. 🧠 Oracle → Gradient Field

### Видаляємо:

```ts
oracle.prune();
oracle.breed();
oracle.tickHomeostasis();
```

---

### Додаємо:

```ts
interface PressureField {
  energyGradient(x, y): number;
  entropyGradient(x, y): number;
  mutationBias(genome): number;
}
```

---

### Використання:

```ts
agent.update({
  pressure: field.local(agent.position),
});
```

---

### Ключ:

- більше нема “рішень”
- є тільки **середовище, яке тисне**

---

## 4. 🧬 Genome → Phenotype separation

### Новий пайплайн:

```ts
genome (AST)
  ↓ compile
phenotype (executable structure)
  ↓ deploy
agent (runtime entity)
```

---

### Структура:

```ts
interface Phenotype {
  execute(env: EnvSlice): Action[];
  cost: number;
  latency: number;
  surface: InteractionSurface;
}
```

---

### Нове:

- один геном → різні фенотипи (через середовище)
- зʼявляється **адаптація без мутації**

---

## 5. ⏳ Local Time Model

### Видаляємо:

```ts
globalTick++;
```

---

### Додаємо:

```ts
class Agent {
  localTime: number;
  dilation: number;

  tick(dt) {
    this.localTime += dt * this.dilation;
  }
}
```

---

### Dilation:

```ts
dilation = f(
  energy,
  entropyGradient,
  interactionDensity,
);
```

---

### Наслідок:

- асинхронна еволюція
- “швидкі” і “повільні” види

---

## 6. ♻️ Replay → Mutation Source

### Було:

```ts
replay.load(snapshot);
```

---

### Стало:

```ts
mutator.fromHistory({
  t1,
  t2,
  strategy: "diff" | "stable-pattern" | "anomaly",
});
```

---

### Приклад:

```ts
const genome = mutator.fromHistory({
  t1: 1200,
  t2: 1800,
  strategy: "stable-pattern",
});
```

---

### Ефект:

- еволюція отримує **памʼять як ресурс**
- виникає щось типу “культури”

---

## 7. ⚡ Energy Model Rewrite

### Видаляємо:

```ts
if (entropy < X) injectEnergy();
```

---

### Додаємо:

```ts
energy = f(
  interactionWork,
  gradientFlow,
  structuralInstability,
);
```

---

### Приклад:

```ts
energy += abs(entropyGradient) * coupling;
energy -= structuralCost;
```

---

### Ключ:

- енергія більше не “дається”
- вона **виникає**

---

## 8. 🎯 Global Intent Field

### Новий шар:

```ts
interface IntentField {
  vector: number[];
  coherence(agent): number;
}
```

---

### Вплив:

```ts
fitness = survivalScore +
  intentField.coherence(agent);
```

---

### Але:

- intent не жорсткий
- це **напрямок, не правило**

---

## 9. 🧩 Interaction Surface (нове ядро)

Це те, чого не вистачало.

```ts
interface InteractionSurface {
  expose(): Signal[];
  receive(signal): Response;
}
```

---

### Ефект:

- агенти взаємодіють не тільки через lattice
- а через **інтерфейси поведінки**

---

# 🔗 Як це зшивається

```ts
loop:
  external = env.sample()

  for agent:
    localPressure = field.sample(agent.pos)

    phenotype = express(agent.genome, external)

    actions = phenotype.execute({
      env: external,
      pressure: localPressure
    })

    agent.apply(actions)
    agent.energy = updateEnergy(agent)

    agent.tick(dt)
```

---

# ⚠️ Критична зміна мислення

Було:

> система = симуляція

Стало:

> система = **учасник середовища**

---

# 💡 Найсильніший ефект

Після цього патчу у тебе зʼявиться:

- агенти, які:

  - не просто виживають
  - а **шукають взаємодію**
- поведінка, яка:

  - не програмується
  - а **виникає на межі середовищ**
- еволюція, яка:

  - не керується
  - а **відбувається**

---

# Якщо хочеш наступний крок

Я можу:

### 1. Зробити **конкретний TS diff** (по твоїх файлах)

або

### 2. Запропонувати **WGSL compute rewrite**

(де pressure field і energy flow реально рахуються на GPU)

або

### 3. Зібрати **мінімальний прототип (≈300 рядків)**

де це вже “дихає”

---

Скажи напрям. Я підхоплю і поведу далі.
