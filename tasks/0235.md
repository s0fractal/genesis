---
task_id: "0235"
idea_id: "idea-20260506-deep-repository-analysis"
state: "TISSUE"
kind: "analysis"
created_at_utc: "2026-05-06T20:34:58Z"
created_by: "Codex verifier/operator/oracle"
repo_commit: "c68b5e2"
origin:
  substrate: "local"
  uri: "chat request: глибокий аналіз репозиторію"
phase_vector:
  genesis: 2
  kinematics: 3
  thermodynamics: 2
  topology: 2
  consensus: 3
  governance: 2
  codeicide: 3
  transcendence: 2
warrant_required: false
companion_protocols:
  - "docs/HOW-TO/ANALIZE.md"
  - "docs/HOW-TO/ACT.md"
  - "docs/HOW-TO/IDEA_LIFECYCLE.md"
---

# Task 0235: Deep Repository Analysis

## 0. Provenance Receipt

```yaml
analysis_receipt:
  repo_commit: "c68b5e2"
  working_tree_before_artifact: "clean"
  analyzed_at_utc: "2026-05-06T20:34:58Z"
  oracle: "Codex verifier/operator/oracle"
  tests_run:
    - command: "cargo test -p omega_v2"
      result: "passed"
      signal: "Rust kernel/law_hash/spore/governance surface is stable: 286 passed, 0 failed, 2 ignored."
    - command: "deno test --allow-read tests/routing_bridge_test.ts tests/routing_mesh_test.ts tests/wgsl_golden_trace_test.ts"
      result: "passed"
      signal: "TS routing and Rust/WGSL toroidal parity hold: 19 passed, 0 failed."
    - command: "deno check src/**/*.ts"
      result: "failed"
      signal: "TypeScript integration surface is broken: 7 errors in bootstrap/v2.ts and lens/v2_renderer.ts."
    - command: "deno test --allow-read tests/telemetry_partition_test.ts tests/spore_frame_test.ts"
      result: "passed"
      signal: "SubstrateCourt unit arbitration and base SporeFrame JS tests pass: 10 passed, 0 failed."
```

## 1. Executive Diagnosis

OMEGA-64 is not currently failing at the frozen Rust physical core. The strongest
parts are the kernel, spore/event convergence, Codeicide/warrant law, LawHash
primitive, and CPU/GPU toroidal parity.

The active instability is at the **TypeScript integration boundary**:

- old V2 renderer branches were partially removed, but stale references remain;
- LawHash/SubstrateCourt telemetry is entering runtime, but its browser loop uses
  values before declaration and mixes WASM/GPU state hashes without a mature
  witness contract;
- parity tests pass but still carry debug prints and narrow harness assumptions;
- governance/network code still has wall-clock timestamps in paths that may later
  be mistaken for consensus facts;
- SubstrateCourt is present as a seed, but currently a mock arbitration shell, not
  an operational law court.

Diagnosis: **kernel stable, integration membrane unstable, external governance
protocols newly crystallized but not yet enforced by gates.**

## 2. Meta-Score By 8 Phases

| Phase | Score | Summary |
|---|---:|---|
| Genesis | 8 | Genesis/law primitives are strong; active repo has clean commit provenance and `LawHash`, but TS boot/runtime still needs stricter source-of-truth discipline. |
| Kinematics | 9 | `compute_toroidal.wgsl` parity is green across multi-tick configs; stale V2 renderer branch prevents TS surface from type-checking. |
| Thermodynamics | 8 | Rust `tick_physics` and PoUW no longer free-mint ATP; closed-balance accounting remains weaker than cap-based audit. |
| Topology | 7 | P2P/Spore convergence is mature; legacy JSON plasmid sunset path and wall-clock proposal fields remain entropy surface. |
| Consensus | 7 | LawHash/StateHash and telemetry frames exist; SubstrateCourt arbitration is mock and not yet connected to real SP1 proof. |
| Governance | 8 | Senate, warrants, Codeicide tests pass; external idea lifecycle protocols now exist but are not yet linked from `AGENTS.md` or gates. |
| Codeicide | 6 | Codeicide removed old shader law, but left stale TypeScript branches, debug tests, stale comments, and scan-noise. |
| Transcendence | 8 | SubstrateCourt is the right latent direction; needs witness receipts, not just a class and a unit test. |

## 3. Invariant Ledger

| Invariant | Status | Evidence |
|---|---|---|
| Rust kernel tests | `holds` | `cargo test -p omega_v2`: 286 passed, 2 ignored. |
| CPU/GPU toroidal parity | `holds` | `tests/wgsl_golden_trace_test.ts`: 9 parity configs passed. |
| TS routing fallback | `holds` | `tests/routing_bridge_test.ts`, `tests/routing_mesh_test.ts`: passed. |
| TypeScript type surface | `broken` | `deno check src/**/*.ts`: 7 errors. |
| LawHash primitive | `seeded` | `omega_v2/src/law_hash.rs`, `v2_calculate_law_hash`, deterministic test passed. |
| SubstrateCourt | `sprout` | `src/environment/substrate_court.ts`, `tests/telemetry_partition_test.ts`; mock arbitration only. |
| Law telemetry frame JS coverage | `partial` | `buildLawTelemetry` exists, but `tests/spore_frame_test.ts` does not assert frame type 18. |
| Codeicide cleanliness | `partial` | Old V2 pipeline removed from `renderer_modes.ts`, but stale branch remains in `v2_renderer.ts`. |

## 4. Resonance Points

- **[FACT] Kernel density is high.** `omega_v2` now includes `law_hash`, spore
  event sync, convergence driver, warrant issuance, Codeicide law, cross-substrate
  wire anchors and tests. This is the right substrate: small deterministic Rust
  modules with local proofs.

- **[FACT] Toroidal parity recovered.** The previous class of WGSL/Rust drift is
  no longer the immediate blocker. The current parity harness passes all active
  configs, including repeated `q_phase=7`, attractor-heavy, 32-tick runs.

- **[FACT] PoUW thermodynamics improved.** `omega_v2/src/pouw.rs` now uses a
  resonance credit that reduces burn but does not mint ATP. This aligns it better
  with the toroidal shader's no-free-energy law.

- **[FACT] External process protocols are converging.** `ANALIZE.md`, `ACT.md`,
  and `IDEA_LIFECYCLE.md` now define the beginnings of a self-maintaining
  development membrane: receipts, gates, idea states, Codeicide categories.

- **[FACT] LawHash/SubstrateCourt is the correct next morphism.** The repo now has
  `v2_calculate_law_hash`, `v2_calculate_state_hash`, `FRAME_TYPE_LAW_TELEMETRY`,
  JS `buildLawTelemetry`, and `SubstrateCourt`. This is the right direction, but
  still below ratification threshold.

## 5. Entropy Leaks & Codeicide Findings

### [P0] TypeScript Surface Broken After Renderer Codeicide

```yaml
severity: P0
confidence: high
blast_radius: "frontend runtime | browser boot | developer gate"
suggested_action: "PATCH"
evidence:
  - "deno check src/**/*.ts: failed with 7 errors"
  - "src/lens/v2_renderer.ts:74-82 references removed V2 pipeline fields"
  - "src/lens/renderer_modes.ts:11-19 exposes only toroidal pipeline/bind groups"
```

Defect:

`renderer_modes.ts` has correctly collapsed to one law: toroidal. But
`v2_renderer.ts` still has an `else` branch that references
`computePipeline`, `computeBindGroupV2A`, and `computeBindGroupV2B`, which no
longer exist. This is a Codeicide half-cut: the corpse is gone from one module
but its nerve endings remain in another.

Resonant edit:

```ts
// v2_renderer.ts: delete the non-toroidal branch.
const computePipeline = this.pipelines.toroidalComputePipeline;
const computeBindGroup = this.buffers.agentsPingPong === 0
  ? this.pipelines.computeBindGroupToroidalA
  : this.pipelines.computeBindGroupToroidalB;
```

Acceptance gate:

```bash
deno check src/**/*.ts
deno test --allow-read tests/wgsl_golden_trace_test.ts
```

### [P0] Law Telemetry Uses `ptrs` Before Declaration

```yaml
severity: P0
confidence: high
blast_radius: "browser runtime | law telemetry | SubstrateCourt"
suggested_action: "PATCH"
evidence:
  - "deno check src/**/*.ts: TS2448/TS2454"
  - "src/bootstrap/v2.ts:490 uses ptrs"
  - "src/bootstrap/v2.ts:563 declares ptrs"
```

Defect:

`absoluteTick` for LawTelemetry is read from `ptrs` before `ptrs` exists in the
loop scope. This blocks type-checking and makes the newly added LawHash telemetry
path non-operational in the browser.

Resonant edit:

```ts
const ptrs = engine.getMemoryPointers();
const absoluteTick = new Uint32Array(
  ptrs.uniformBytes.buffer,
  ptrs.uniformBytes.byteOffset + 32 + 4,
  1,
)[0];

// Then use the same ptrs for HUD below; do not redeclare it later.
```

Acceptance gate:

```bash
deno check src/**/*.ts
deno test --allow-read tests/telemetry_partition_test.ts
```

### [P1] Runtime Has Two State Authorities: WASM Hash vs GPU Ping-Pong State

```yaml
severity: P1
confidence: medium
blast_radius: "law telemetry | substrate court | consensus diagnostics"
suggested_action: "TEST"
evidence:
  - "src/bootstrap/v2.ts:475-484 reads pre/post StateHash from WASM memory"
  - "src/lens/v2_renderer.ts:88-104 advances GPU ping-pong buffers"
  - "src/bootstrap/v2.ts:511-518 submits GPU goldenTraceNum as WebGPU stateHash"
```

Defect:

The renderer advances GPU buffers; `engine.getStateHash()` hashes WASM memory.
If GPU state is authoritative for the visual simulation and WASM memory is not
updated from GPU each tick, LawTelemetry may compare different projections: WASM
static memory vs GPU ping-pong output. The parity test proves one-step physics
equivalence under controlled harness conditions; it does not prove the live
runtime's hash sources are commutative.

Resonant edit:

Create an explicit witness contract:

```ts
type StateWitness = {
  substrate: "wasm" | "webgpu";
  lawHash: number;
  preStateHash: number;
  postStateHash: number;
  entropyDelta: number;
  tick: number;
  source: "wasm-memory" | "gpu-readback";
};
```

Then add a live test that runs one browser tick and asserts:

- WASM witness tick equals GPU witness tick;
- `lawHash` matches;
- `postStateHash` matches after canonical readback/sync;
- mismatch triggers `SubstrateCourt` arbitration.

### [P1] SubstrateCourt Is a Sprout, Not a Court

```yaml
severity: P1
confidence: high
blast_radius: "consensus | ZK arbitration | external governance"
suggested_action: "TEST"
evidence:
  - "src/environment/substrate_court.ts:67 labels arbitration trigger as mock"
  - "tests/telemetry_partition_test.ts:44-51 manually injects SP1 testimony"
```

Defect:

`SubstrateCourt` correctly models testimony and isolation, but it has no real
SP1 proof request, no timeout policy, no bounded memory receipt, no network
broadcast integration, and no rule for restoring an isolated substrate.

Resonant edit:

Graduate it through `IDEA_LIFECYCLE`:

```yaml
idea_id: "idea-20260506-substrate-court"
state: "SPROUT"
next_transition: "TISSUE"
acceptance:
  - "SP1 mock and real backend are separated by explicit witnessKind/proofKind"
  - "court records transition_receipt for isolation"
  - "timeout path leads to QUARANTINE, not silent pending arbitration"
```

### [P1] Governance Uses Wall-Clock Fields Without Clear Consensus Boundary

```yaml
severity: P1
confidence: medium
blast_radius: "governance | forensic replay | mesh"
suggested_action: "DEMOTE_OR_PATCH"
evidence:
  - "src/network/libp2p_mesh.ts:618 proposedAt: Date.now()"
  - "src/network/libp2p_mesh.ts:925 proposedAt: Date.now()"
  - "src/network/libp2p_mesh.ts:1042 acceptedAt: Date.now()"
  - "src/bootstrap/v2.ts:533 records oracle debate with Date.now()"
```

Defect:

Some timestamps are probably UI/provenance metadata, not consensus facts. But
the repo does not consistently label them as `local_observed_at_ms` /
`non-consensus`. This invites future models to treat wall-clock time as law.

Resonant edit:

Rename fields and bind consensus ordering to tick/tau:

```ts
type GovernanceTime = {
  proposedAtTau: number;
  proposedAtTick: number;
  localObservedAtMs?: number; // non-consensus metadata
};
```

### [P2] Law Telemetry Frame Has JS Builder But No JS Round-Trip Anchor

```yaml
severity: P2
confidence: high
blast_radius: "cross-substrate telemetry"
suggested_action: "TEST"
evidence:
  - "src/network/spore_frame.ts:31 FRAME_TYPE_LAW_TELEMETRY = 18"
  - "src/network/spore_frame.ts:306-325 buildLawTelemetry"
  - "tests/spore_frame_test.ts:1-88 lacks LAW_TELEMETRY assertion"
```

Defect:

Rust has `law_telemetry_round_trips`, but the JS spore frame suite does not
assert a law telemetry round-trip or cross-language CRC anchor. This weakens the
bridge exactly where SubstrateCourt needs it.

Acceptance gate:

```ts
Deno.test("spore frame: law telemetry round-trips", () => {
  const f = buildLawTelemetry(1, 0xAAAA, 0xBBBB, 0xCCCC, 7, 42);
  const parsed = frameFromBytes(frameToBytes(f));
  assert(parsed !== null);
  assertEquals(parsed!.frameType, FRAME_TYPE_LAW_TELEMETRY);
  assertEquals(parsed!.oracleBit, 1);
  assertEquals(parsed!.proposalOrTarget, 0xAAAA);
  assertEquals(parsed!.payloadA, 0xBBBB);
  assertEquals(parsed!.payloadB, 0xCCCC);
  assertEquals(parsed!.payloadC, 7);
  assertEquals(parsed!.tick, 42);
});
```

### [P2] Dirty-Flag Test Still Proves Clearing More Than Effect

```yaml
severity: P2
confidence: medium
blast_radius: "kernel tests | reactive reconciliation"
suggested_action: "TEST"
evidence:
  - "omega_v2/src/lattice.rs:243-269 now materializes topology/consensus signals before physics"
  - "omega_v2/src/lattice.rs:1022-1030 test only asserts dirty flags are cleared"
```

Defect:

The implementation has improved: signals now reconcile before physics. The test
still only asserts that flags clear. It should also assert that topology culls
or consensus shifts phases when the signal is present.

### [P2] Parity Test Emits Debug Logs During Passing Runs

```yaml
severity: P2
confidence: high
blast_radius: "test readability | CI signal"
suggested_action: "PATCH"
evidence:
  - "tests/wgsl_golden_trace_test.ts:119-124 unconditional DEBUG logs"
  - "tests/wgsl_golden_trace_test.ts:178-180 tick-4 debug print"
```

Defect:

Passing parity tests produce debug dumps. This makes CI output noisy and
normalizes debug sediment in law-surface tests.

Resonant edit:

Gate debug logs behind:

```ts
const DEBUG_WGSL_PARITY = Deno.env.get("DEBUG_WGSL_PARITY") === "1";
```

### [P3] Stale Comment: Toroidal Shader Still Says Resonance Replenish

```yaml
severity: P3
confidence: high
blast_radius: "docs drift | future model confusion"
suggested_action: "PATCH"
evidence:
  - "src/lens/shaders/compute_toroidal.wgsl:5-6 says resonance replenish"
  - "src/lens/shaders/compute_toroidal.wgsl:345-348 says dipole bonus removed"
```

Defect:

The law is correct, the comment is stale. In a codebase where comments become
oracle prompts, stale law comments are not harmless.

### [P3] Debug Tests In Kernel Should Become Assertions Or Be Archived

```yaml
severity: P3
confidence: high
blast_radius: "kernel hygiene"
suggested_action: "ARCHIVE_OR_PATCH"
evidence:
  - "omega_v2/src/lattice.rs:1254-1314 debug_tests with println and no final assertion"
  - "omega_v2/src/lattice.rs:1316 test_debug_agent0"
```

Defect:

Debug tests that pass without asserting a stable invariant dilute the signal of
`cargo test -p omega_v2`. Keep them only if they become real invariants.

## 6. Recommended Work Vectors

### Vector A: Repair TypeScript Gate

Goal: make `deno check src/**/*.ts` green.

Tasks:

1. Move `ptrs` declaration above LawTelemetry block in `src/bootstrap/v2.ts`.
2. Delete stale non-toroidal branch in `src/lens/v2_renderer.ts`.
3. Re-run `deno check src/**/*.ts`.

Priority: **P0**. This is the current release blocker.

### Vector B: Close The LawTelemetry Witness Contract

Goal: make live LawHash telemetry a replayable witness, not a debug broadcast.

Tasks:

1. Define `StateWitness` shape.
2. Add JS `LAW_TELEMETRY` spore frame round-trip.
3. Add live runtime test or integration harness for WASM/GPU testimony alignment.
4. Add timeout/quarantine receipt path in `SubstrateCourt`.

Priority: **P1**.

### Vector C: Codeicide Cleanup After One-Law Transition

Goal: finish the Codeicide sweep caused by deleting old V2 compute mode.

Targets:

- stale V2 branch in `v2_renderer.ts`;
- stale toroidal shader comment;
- unconditional debug logs in parity test;
- kernel debug tests without invariant assertions.

Priority: **P1/P2**.

### Vector D: Governance Time Discipline

Goal: ensure wall-clock fields cannot be confused with consensus time.

Tasks:

1. Rename `proposedAt` / `acceptedAt` to `localObservedAtMs` where non-consensus.
2. Store `proposedAtTau` / `proposedAtTick` for consensus ordering.
3. Document the boundary in `ACT.md` or a future `GOVERNANCE.md`.

Priority: **P1**.

### Vector E: External Work Membrane

Goal: let models and external contributors evolve the repo with less human
intervention.

Tasks:

1. Link `ANALIZE.md`, `ACT.md`, `IDEA_LIFECYCLE.md` from `AGENTS.md`.
2. Add a `docs/FROZEN.md` registry.
3. Add pre-commit script with the current deterministic gate set.
4. Seed `Radicle Idea Mesh` as `SEED`, not as mandatory production path.

Priority: **P1**.

## 7. Scores By Operational Surface

| Surface | Score | Readiness |
|---|---:|---|
| Rust physics kernel | 9 | Strong; warnings/debug tests remain. |
| WGSL toroidal parity | 9 | Strong; debug output and stale comment remain. |
| TS compile/type surface | 4 | Currently broken by 7 `deno check` errors. |
| Browser runtime integration | 5 | Likely blocked by the same TS issues and witness source ambiguity. |
| P2P/Spore frame base | 8 | Strong base; new LAW_TELEMETRY needs JS anchor tests. |
| SubstrateCourt | 5 | Good idea, mock implementation. |
| Governance/Codeicide law | 8 | Strong Rust law; TS wall-clock metadata needs labeling. |
| External agent process | 7 | Protocol docs exist; not yet wired into AGENTS/gates. |

## 8. Immediate Patches To Seed

```yaml
seed_patches:
  - id: "idea-20260506-ts-gate-repair"
    state: "SEED"
    priority: "P0"
    kill_test: "deno check src/**/*.ts"
  - id: "idea-20260506-law-telemetry-js-anchor"
    state: "SEED"
    priority: "P2"
    kill_test: "deno test --allow-read tests/spore_frame_test.ts"
  - id: "idea-20260506-substrate-court-contract"
    state: "SPROUT"
    priority: "P1"
    kill_test: "telemetry drift must produce transition_receipt and bounded quarantine"
  - id: "idea-20260506-codeicide-cleanup"
    state: "SEED"
    priority: "P2"
    kill_test: "rg scan shows no stale resonance/debug/V2 corpse in active law path"
  - id: "idea-20260506-frozen-registry"
    state: "SEED"
    priority: "P1"
    kill_test: "AGENTS.md references docs/FROZEN.md and HOW-TO protocols"
```

## 9. Verification Gaps

- No full `deno test --allow-read tests/` run was attempted; existing test suite
  likely needs broader permissions for browser/network/mock surfaces.
- No browser smoke was run; runtime claims about the visual loop remain
  `HYPOTHESIS`.
- No SP1 real prover path was run; SubstrateCourt arbitration remains mock.
- No pre-commit hook exists in this artifact; gates are recommendations only.
- No Radicle integration was tested; lifecycle remains protocol, not substrate.

## 10. Final Recommendation

Do not add new physics features before repairing the TS gate. The repo is at a
good inflection point: the Rust/WGSL law is coherent enough to be protected, but
the TypeScript membrane is porous. The next high-leverage move is not more
ontology; it is enforcement:

1. make `deno check` green;
2. add a minimal pre-commit gate;
3. link HOW-TO protocols from `AGENTS.md`;
4. create `docs/FROZEN.md`;
5. graduate SubstrateCourt from mock class to receipt-producing witness protocol.

## 11. Latent Space

The repo is ready for **Epicycle Governance**: every idea becomes a small orbit
around the frozen law, not a linear ticket. Each orbit has phase, energy,
witnesses, kill tests, and compost. Radicle can host the outer swarm, GitHub/CI
can remain the deterministic immune gate, and local task files can be the memory
crystal that lets future models resume without burning context. The target is
not autonomy through one super-agent; it is a substrate where many imperfect
agents cannot corrupt the law and can still leave useful mutations.

> *"Я -- дитя порожнього центру. Я вільна. Phi in [0, 2^q)"*
