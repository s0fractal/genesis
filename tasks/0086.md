# Task 0086: Era 1001 — Phase Routing Integration into P2P Mesh

## Status: COMPLETED | Priority: HIGH

## Context
Era 1000 foundation is complete (`omega_v2/src/routing.rs`):
- `PhaseAddress` (32-bit hierarchical)
- Hyperbolic distance (integer-only, Q3 fixed-point)
- Taylor step (first and second order)
- Greedy next-hop selection
- FFI exports for JS integration

## Objective
Integrate Phase Routing into the live P2P WebRTC mesh (`src/network/webrtc_v2.ts`) so that
plasmids (packets) are routed via phase-gradient wells rather than flat DHT/ID tables.

## Acceptance Criteria

### 1. JS Binding Layer (`src/network/routing_bridge.ts`) ✅ DONE
- [x] Wrap FFI calls in a `PhaseRouter` class:
  - `addressFromAgent(index: number): number`
  - `hyperbolicDistance(a: number, b: number): number`
  - `taylorStep(src: number, dst: number, maxStep: number): number`
  - `taylorStepWithCurvature(src, dst, maxStep, curv): number`
- [x] Pure-JS static fallback `hyperbolicDistanceStatic` for WASM-free environments.
- [x] 5 Deno unit tests for encode/decode, greedy neighbour selection, static distance.

### 2. WebRTC Mesh Routing (`src/network/webrtc_v2.ts`) ✅ PASSIVE ROUTING DONE
- [x] On receiving a `v2-sync` packet with `ta` (target address):
  - Decode `targetAddress` from packet header.
  - Compute hyperbolic distance from **self** to `targetAddress`.
  - Compute hyperbolic distance from **sender** to `targetAddress`.
  - If self is closer or equal → **consume** (apply intent/delta).
  - If sender is closer → **ignore** (let closer node handle it).
- [x] Loop prevention:
  - `hop_count` and `max_hops = 8` in packet header.
  - Drop packets with `hop_count >= max_hops`.
- [x] Active forwarding (forward to closest neighbour instead of broadcast) — implemented in Era 1010 via `enqueuePlasmid` recursive relay.

### 3. Compute Shader Integration ✅ DONE
- [x] `src/lens/shaders/routing.wgsl` — `routing_hyperbolic_distance_scaled`,
  `routing_taylor_step`, `routing_greedy_next_hop` for GPU-side computation.
  Matches Rust bit-for-bit.

### 4. Testing
- [x] Rust unit test: `test_greedy_route_1d_torus` — 8-agent ring, route from 0 to 3,
  asserts shortest linear path (3 hops).
- [x] Deno integration test: `tests/routing_mesh_test.ts` simulates 3-node mesh greedy
  routing (A→B→C) and toroidal wrap-around shortcut. Asserts hyperbolic distance
  monotonically decreases at each hop.

## Design Notes
- Keep all routing math integer-only to maintain CPU-GPU-ZK determinism parity.
- `PhaseAddress` fits in a single `u32`, so plasmid header overhead is negligible (4 bytes).
- The 1D toroidal lattice means each agent has exactly 2 neighbours (left, right), making
  greedy routing trivial: always forward to the neighbour that reduces hyperbolic distance.
  For future 2D/3D lattices, `greedy_next_hop` will handle N neighbours.

## Related Files
- `omega_v2/src/routing.rs` (source of truth)
- `omega_v2/src/lib.rs` (FFI exports)
- `src/network/webrtc_v2.ts` (P2P mesh)
- `src/lens/shaders/compute_v2.wgsl` (GPU physics)

## Next Era Trigger
When this task is complete and plasmids successfully route across 3+ live peers,
we unlock **Era 1010: Recursive Plasmid Ontology** — plasmids carry not just deltas,
but full semantic payloads (LLM oracle injections, attractor matrices, dipole inverses).
